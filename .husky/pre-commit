#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

set +e  # Disable exit on error for lint checks

# Run lint:fix to auto-fix issues (ignore exit code)
(cd magni && pnpm run lint:fix) 2>&1 || true

# Run lint to check for actual errors (lint:fix exit code/output is unreliable for warnings vs errors)
cd magni || exit 1
LINT_OUTPUT=$(pnpm run lint 2>&1)

# ESLint summary line: "✖ N problems (E errors, W warnings)" — only fail when E > 0
HAS_ERRORS=$(echo "$LINT_OUTPUT" | grep -oE "✖ [0-9]+ problems \([1-9][0-9]* errors" || true)

if [ -n "$HAS_ERRORS" ]; then
  echo "❌ Linting failed with errors. Please fix the errors before committing."
  echo "$LINT_OUTPUT"
  set -e
  exit 1
fi

HAS_WARNINGS=$(echo "$LINT_OUTPUT" | grep -oE "✖ [0-9]+ problems" || true)
if [ -n "$HAS_WARNINGS" ]; then
  echo "⚠️  Linting completed with warnings (no errors). Allowing commit."
else
  echo ""
  echo "✅ Linting passed (warnings are allowed)"
fi

cd "$(git rev-parse --show-toplevel)" || true

git add -u 2>/dev/null || true

STAGED_GO_FILES=$(git diff --cached --name-only --diff-filter=ACM | grep '^modi/.*\.go$')
if [ -n "$STAGED_GO_FILES" ]; then
    echo "Running golangci-lint on staged Go files in modi/..."
    
    # Add Go bin to PATH if it exists
    if [ -d "$HOME/go/bin" ]; then
        export PATH="$PATH:$HOME/go/bin"
    fi
    
    # Try to find golangci-lint in common locations
    GOLANGCI_LINT=""
    if command -v golangci-lint >/dev/null 2>&1; then
        GOLANGCI_LINT="golangci-lint"
    elif [ -f "$HOME/go/bin/golangci-lint" ]; then
        GOLANGCI_LINT="$HOME/go/bin/golangci-lint"
    elif command -v go >/dev/null 2>&1 && [ -f "$(go env GOPATH)/bin/golangci-lint" ]; then
        GOLANGCI_LINT="$(go env GOPATH)/bin/golangci-lint"
    fi
    
    # Check if golangci-lint is available
    if [ -z "$GOLANGCI_LINT" ]; then
        echo "Warning: golangci-lint is not installed or not in PATH"
        echo "Skipping Go lint check. Install with: curl -sSfL https://raw.githubusercontent.com/golangci/golangci-lint/master/install.sh | sh -s -- -b \$(go env GOPATH)/bin latest"
    else
        cd modi || exit 0
        set -e  # Re-enable exit on error for Go lint check
        if ! "$GOLANGCI_LINT" run --timeout=5m ./...; then
            echo ""
            echo "❌ Go linting failed! Please fix the errors before committing."
            echo "You can run 'make lint' in the modi/ directory to see the errors."
            exit 1
        fi
        set +e  # Disable exit on error again after Go lint passes
        echo "✅ Go linting passed!"
    fi
fi

# Make sure we exit successfully if we got here
exit 0

